# Local development — PostgreSQL + sample seed data
spring.datasource.url=jdbc:postgresql://localhost:5432/jamipariseva
spring.datasource.username=postgres
spring.datasource.password=Arup18@@
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
# Set to never to prevent deleting/resetting database data on restart. Set to always only when you want to re-seed.
spring.sql.init.mode=never
spring.sql.init.data-locations=classpath:data.sql

logging.level.com.jamipariseva=DEBUG
